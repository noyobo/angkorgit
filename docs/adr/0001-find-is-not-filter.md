# Find is not filter

The Graph used to treat text search as a filter: matching commits became a flat list and the lanes disappeared. That read as broken. Find now walks the same history the Graph already shows and jumps between matches; the only filter left is the branch.

**Status**: accepted
