# A worktree is a repository

A linked Worktree is opened as its own Repository tab, with its own Working copy, Graph, drafts and terminal. The Sidebar still lists Worktrees as children of the current Repository so you can jump between folders that share one `.git`. Checking out a branch another Worktree holds switches to that tab instead of moving HEAD.

**Status**: accepted
